import { PendingPage } from '@/components/shell/PendingPage';

export default function Page() {
  return (
    <PendingPage
      kicker="THE LIBRARIES"
      title="Catalog"
      sub="Five libraries — Nutrition, Fitness, Yoga, Mind Wellness and Motivation — and the templates built from them."
      icon="bookmark"
      sentence="The catalogue lands here."
      detail="Motivation is a library and a template kind, still not a fifth pillar."
    />
  );
}
