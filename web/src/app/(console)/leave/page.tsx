import { PendingPage } from '@/components/shell/PendingPage';

export default function Page() {
  return (
    <PendingPage
      kicker="TIME OFF"
      title="Time & Cover"
      sub="Who is away, who is covering, and what that moves."
      icon="cal"
      sentence="The leave board lands here."
      detail="An approved cover moves the seat AND the appointment from one write, so every screen agrees."
    />
  );
}
