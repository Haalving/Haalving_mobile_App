import { PendingPage } from '@/components/shell/PendingPage';

export default function Page() {
  return (
    <PendingPage
      kicker="THE COMMUNITY"
      title="Community"
      sub="The console side of the client's Community tab — gatherings, challenges and what the house announces."
      icon="tribe"
      sentence="The community board lands here."
      detail="Broadcasts are composed here and delivered into each client's own circle."
    />
  );
}
