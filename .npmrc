# Expo and React Native resolve peers loosely; strict mode breaks the mobile install.
strict-peer-dependencies=false
auto-install-peers=true
node-linker=hoisted

# esbuild (via vitest) and prisma need their postinstall to run
enable-pre-post-scripts=true
