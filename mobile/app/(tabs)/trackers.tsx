import { TabScreen } from '@/components/ui/TabScreen';

export default function Screen() {
  return (
    <TabScreen
      kicker="YOUR READINGS"
      title="Trackers"
      sub="Steps, water, sleep, screen time — and the Journey inside."
      icon="pulse"
      sentence="Your trackers land here."
      detail="Each reading is an instrument, with three weeks of history behind it."
    />
  );
}
